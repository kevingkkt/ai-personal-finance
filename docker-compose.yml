services:
  front-page:
    build:
      context: ./front-page
    container_name: finance-front-page
    ports:
      - "8080:80"
  bills_database:
    build:
      context: ./Luke/database
    container_name: finance-database
    ports:
      - "5002:5002"